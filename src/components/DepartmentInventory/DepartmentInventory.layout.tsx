import React, { forwardRef } from "react";
import { Table, TableHeader, TableColumn, TableBody, TableRow, TableCell, Input, Button, Modal, ModalContent, ModalHeader, ModalBody, ModalFooter, useDisclosure, Autocomplete, AutocompleteItem, Chip, Switch } from "@heroui/react";
import { Icon } from "@iconify/react";

export const DepartmentInventoryLayout = forwardRef((props: any, ref) => {
  // ...разметка компонента, все пропсы и состояния передаются через props...
  // ...сюда переносится только JSX без логики...
  return (
    <div>{/* ...сюда вставить разметку из оригинального компонента... */}</div>
  );
});

DepartmentInventoryLayout.displayName = "DepartmentInventoryLayout";
